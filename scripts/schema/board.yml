type: object
properties:
  board:
    type: object
    properties:
      toolchain:
        type: string
      kernel:
        type: string
      kernel_config:
        type: string
      uboot:
        type: string
      uboot_config:
        type: string
      uboot_script:
        type: string
      image:
        type: string
      linux_dtb:
        type: string
      linux_image:
        type: string
      uboot_image:
        type: string
      root:
        type: string
      xen:
        type: string
      xen_config:
        type: string
      xen_arch:
        type: string
      xen_image:
        type: string

    required:
      - toolchain
      - kernel
      - kernel_config
      - uboot
      - uboot_config
      - uboot_script
      - image
      - linux_dtb
      - linux_image
      - uboot_image
      - root
